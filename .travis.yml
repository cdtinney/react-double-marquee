language: node_js
node_js:
  - lts/*

jobs:
  include:
    # Define the release stage that runs semantic-release
    - stage: release
      node_js: lts/*
      # No tests -- skip
      script: skip
      deploy:
        if: branch = master
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release
